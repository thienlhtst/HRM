import { PipeSharePipe } from './pipe-share.pipe';

describe('PipeSharePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSharePipe();
    expect(pipe).toBeTruthy();
  });
});
