export interface Requestpaging {
    keyword:string
    pageindex:number
    pagesize:number
}
export interface RequestHomepaging {
    keyword:string
    flag:number
    pageindex:number
    pagesize:number
}
